comments:
allowedFields: [name, email, message]
requiredFields: [name, message]
branch: "main" # samma som i _config.yml
format: yml # skapar .yml-filer
path: "_data/comments/{options.slug}"
filename: "{ISO-8601}"
moderation: true # du godkänner innan syns
generatedFields:
date:
type: date
options:
format: iso8601
transforms:
email: md5 # hash av e‑post (valfritt)
